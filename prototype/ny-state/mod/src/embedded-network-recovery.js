// Replaced at build time with the compressed global network recovered from
// the timestamped pre-session-binding backup.
export const embeddedNetworkRecoveryGzipBase64 = __NY_NETWORK_RECOVERY_GZIP_BASE64__;
